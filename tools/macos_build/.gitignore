raw_package/
installer/
*.pkg
*.gz

resources/
FastSurferPackageContent/
# the installer scripts are now generated into their own directory rather than into scripts/, so
# nothing generated lands in the tracked source tree
pkg-scripts/
# standalone python distributions fetched by uv for bundling
.uv-pythons/
# generated by pkgbuild --analyze to pin bundle components to their packaged location
component.plist
# generated from FastSurfer.applescript.template and FastSurfer.iconset for the applet
FastSurfer.applescript
FastSurfer.iconset/
